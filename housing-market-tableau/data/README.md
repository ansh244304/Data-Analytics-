# /data

This folder holds the raw dataset downloaded from Kaggle.

**Dataset:** [Transformed Housing Data 2](https://www.kaggle.com/datasets/rituparnaghosh18/transformed-housing-data-2)

Run `python data/download_data.py` from the project root to populate this folder.

> CSV files are excluded from git via `.gitignore` to keep the repo size small.
